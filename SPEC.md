# 운임 정산 명세

## 견적 (quote)

주문 하나에 대한 최종 청구 금액을 계산한다. 단위는 원(정수)이며, 소수가 나오면 반올림한다.

## 할인 정책

- 모든 할인은 **기본 운임(`basePrice`)을 기준으로** 각각 계산한다.
- 계산된 할인액을 **합산해서 기본 운임에서 한 번에** 차감한다.
- **할인끼리 중첩 적용하지 않는다.** 즉 이미 할인된 금액에 다시 할인율을 곱하지 않는다.

### 예시

```
basePrice      10,000
쿠폰           1,000원 정액
멤버십         10% 할인

쿠폰 할인액    = 1,000
멤버십 할인액  = 10,000 × 10% = 1,000     ← 기본 운임 기준. 9,000 기준이 아님.

최종 청구액    = 10,000 - 1,000 - 1,000 = 8,000
```

> ⚠️ `(10,000 - 1,000) × 0.9 = 8,100` 은 **잘못된 계산이다.**
> 멤버십 할인이 쿠폰 적용 후 금액에 걸리면 고객이 받아야 할 할인을 덜 받게 된다.

## 할인 종류

| 종류 | 필드 | 할인액 계산 |
|---|---|---|
| 쿠폰 | `couponAmount` | 그 값 그대로 |
| 멤버십 | `membershipRate` | `basePrice × (1 - membershipRate)` |

`membershipRate` 는 적용 후 비율이다. 0.9 는 10% 할인을 뜻한다.

```
basePrice 10,000 · membershipRate 0.9
멤버십 할인액 = 10,000 × (1 - 0.9) = 1,000
멤버십만 적용한 금액 = 10,000 - 1,000 = 9,000
```

## 부가세

청구서를 발행할 때 **할인이 모두 끝난 금액**에 10%를 가산한다.

```
할인 후 금액   8,000
부가세         8,000 × 10% = 800
청구서 금액    8,800
```

부가세는 할인보다 뒤에 온다. 순서가 뒤집히면 세액이 달라진다.

## 미구현

현재 `Order` 모델에는 `couponAmount` 와 `membershipRate` 필드가 들어오지만,
계산 로직은 아직 붙어 있지 않다. 두 할인과 부가세 모두 별도 티켓으로 진행 중이다.
